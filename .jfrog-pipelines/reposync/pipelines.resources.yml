template: true   # required for local templates.
valuesFilePath: ./values.yml

resources:
  - name: sync_cron
    type: CronTrigger
    configuration:
      interval: '0 */2 * * *'     # every 2 hours

