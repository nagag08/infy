template: true   # required for local templates
valuesFilePath: ./values.yml

resources:

  - name: {{ .Values.pipelines.name }}
    type: PropertyBag
    configuration:
      commitSha: 1
      runID: 1
